# Versi dipin, bukan ">=" — alasannya sama dengan repo Screening-Saham:
# "jalan di sini, rusak di CI" harus bisa dibedakan dari perubahan perilaku
# sumber data. Naikkan versinya secara sadar, satu per satu.
#
# Versi diperiksa di PyPI pada 14 Sep 2026.
yfinance==1.7.0
pandas==3.0.5
numpy==2.5.3
requests==2.34.2
# lxml: parser HTML untuk pandas.read_html (tabel konstituen di Wikipedia).
lxml==6.1.3
# Kalender NYSE: libur, tutup setengah hari, jam tutup per tanggal. Dipakai
# untuk memutuskan apakah bar hari ini sudah final.
pandas_market_calendars==5.4.0
